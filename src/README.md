# Source Scripts

Optional scripts for reproducibility checks can live here.

Use scripts for:

- input inventory,
- CRS and geometry validation,
- independent descriptive checks,
- sensitivity tables,
- figure/table export for the full-paper phase.

The main analytical workflow may still be QGIS/PlanX-driven; scripts should support verification rather than duplicate every plugin tool.

